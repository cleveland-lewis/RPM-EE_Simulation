## Update — 2025-11-20

- Refactored `src/simulation.py:fit_nc_mcm` into testable units with input validation, R-hat convergence check, RMSE mean+95% CI computation, plot saving (PNG/PDF), artifact pickling, JSON summary export, and logging-based reporting; restored `_SIM_OVERRIDES` after each run to avoid hidden state.
- Aligned `config/nc_mcm_model.build_nc_mcm` with reproducible sampling by accepting `random_seed` for both posterior draws and PPC, matching the refactored caller’s seed handling.
- Added `src/test_fit_nc_mcm.py` to cover validation errors, RMSE CI math, and R-hat detection; pytest currently blocked in this sandbox because `scipy` (sensory dependency) is unavailable—installing SciPy should allow reruns.
- Resolved [A1] by designating `src/simulation.py` as the canonical engine and turning `src/presets.py.run_simulation` into a thin wrapper around it; preserved the legacy teaching loop as `_legacy_run_simulation` for educational reading.
- Resolved [A2] by moving the NC-MCM pipeline into `src/nc_mcm_model.py`, making `config/nc_mcm_model` a thin re-export, updating `fit_nc_mcm` to import from `src`, and removing `sys.path` mutation from `config/batch_runner.py` so `config` cleanly depends on the library layer.
- Tackled [A4] by introducing a shared Pydantic schema (`src/config_schema.py`) for simulation configs, wiring FastAPI’s `/config` endpoint to serve/save validated models (`src/config.py`), and updating `config/batch_runner.py` to normalize CLI/preset/JSON inputs through the same schema before dispatching runs.
- Updated `/docs/progress/a.i._instructions/AGENTS.md`
- Updated routine flow to update imports and test the model.
